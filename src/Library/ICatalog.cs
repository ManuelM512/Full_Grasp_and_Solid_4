namespace Full_GRASP_And_SOLID;
public interface ICatalog{
    public void Add(string description, int cost);

    //public Iitem GetIitem(string description);
    //public Iitem IitemAt(int index);
}